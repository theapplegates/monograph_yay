# Public cloud name, used to build delivery URLs. Set this in your build host too.
PUBLIC_CLOUDINARY_CLOUD_NAME=

# Upload command only. Never use a PUBLIC_ prefix for these secrets.
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
